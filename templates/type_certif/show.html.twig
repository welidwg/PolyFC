{% extends 'base.html.twig' %}

{% block title %}Types de Certification

{% endblock %}

{% block content %}
	<h1>Les Types de Certification</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ type_certif.id }}</td>
			</tr>
			<tr>
				<th>Libelle</th>
				<td>{{ type_certif.libelle }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_type_certif_index') }}">back to list</a>

	<a href="{{ path('app_type_certif_edit', {'id': type_certif.id}) }}">edit</a>

	{{ include('type_certif/_delete_form.html.twig') }}
{% endblock %}
