<form method="post" class="mx-2" action="{{ path('app_type_certif_delete', {'id': type_certif.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_certif.id) }}">
	<button class="btn btn-danger">Supprimer</button>
</form>
