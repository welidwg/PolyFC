<form method="post" action="{{ path('app_session_certif_delete', {'id': session_certif.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ session_certif.id) }}">
	<button class="btn btn-danger">Supprimer</button>
</form>
